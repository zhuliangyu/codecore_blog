<% @posts.each do |e| %>
    <h3><%= e.title %></h3>
    <h3><%= e.body %></h3>
    <h3><%= link_to "Show",post_path(e) %></h3>
    <h3><%= link_to "Edit",edit_post_path(e)
    %></h3>
    <h3><%= link_to "Delete", post_path(e), method: :delete, data: {confirm: "Are you sure"} %>
    </h3>

    <hr>
<% end %>
